apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "podfetch.fullname" . }}-test-connection"
  labels:
    {{- include "podfetch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "podfetch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
