apiVersion: v1
kind: Service
metadata:
  name: {{ include "podfetch.fullname" . }}
  labels:
    {{- include "podfetch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podfetch.selectorLabels" . | nindent 4 }}
